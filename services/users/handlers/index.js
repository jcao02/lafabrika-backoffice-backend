'use strict';

const userNewHandler = async (request, h) => {
};

const userEditHandler = async (request, h) => {
};

const userShowHandler = async (request, h) => {
};

const userListHandler = async (request, h) => {
};

const userDeleteHandler = async (request, h) => {
};


module.exports = {
  userNewHandler,
  userEditHandler,
  userShowHandler,
  userListHandler,
  userDeleteHandler
};
